# Application dependencies

require:
    - play
    - play -> force 0.1
    
repositories:
    - My modules:
        type:       local
        artifact:   "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - play -> force *
