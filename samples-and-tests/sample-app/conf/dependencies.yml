# Application dependencies

require:
    - play 1.2.4
    - force -> force 0.5
    
repositories:
    - MyModules:
        type:       local
        artifact:   "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - force -> force 0.5
