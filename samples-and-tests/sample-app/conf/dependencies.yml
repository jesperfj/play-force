# Application dependencies

require:
    - play
    - force -> force 0.2
    
repositories:
    - MyModules:
        type:       local
        artifact:   "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - force -> force 0.2
